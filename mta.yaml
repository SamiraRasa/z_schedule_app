_schema-version: "3.2"
ID: atzetappmscheduleupload
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: atzetappmscheduleupload-destination-content
  type: com.sap.application.content
  requires:
  - name: atzetappmscheduleupload-destination-service
    parameters:
      content-target: true
  - name: atzetappmscheduleupload-repo-host
    parameters:
      service-key:
        name: atzetappmscheduleupload-repo-host-key
  - name: atzetappmscheduleupload-uaa
    parameters:
      service-key:
        name: atzetappmscheduleupload-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: atzetappmscheduleupload_html_repo_host
          ServiceInstanceName: atzetappmscheduleupload-html5-service
          ServiceKeyName: atzetappmscheduleupload-repo-host-key
          sap.cloud.service: atzetappmscheduleupload
        - Authentication: OAuth2UserTokenExchange
          Name: atzetappmscheduleupload_uaa
          ServiceInstanceName: atzetappmscheduleupload-xsuaa-service
          ServiceKeyName: atzetappmscheduleupload-uaa-key
          sap.cloud.service: atzetappmscheduleupload
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: atzetappmscheduleupload-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: atzetappmscheduleupload-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - atzetappmscheduleupload.zip
      name: atzetappmscheduleupload
      target-path: resources/
- name: atzetappmscheduleupload
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: atzetappmscheduleupload-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: atzetappmscheduleupload-destination-service
    service-plan: lite
- name: atzetappmscheduleupload-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: atzetappmscheduleupload-xsuaa-service
    service-plan: application
- name: atzetappmscheduleupload-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: atzetappmscheduleupload-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
